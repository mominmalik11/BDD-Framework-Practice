package com.bdd.learning.Steps_to_Install;

public class Create_A_Cucumber_Project {
	
	//step 1 Create a maven project
	//step 2 Add the cucumber dependencies (four of them from cucumber io and make sure all four versions are the same)
	//step 3 download cucmber pluggin from the eclipse market place from help dropdown menu and install and restarr eclipse when prompted 
	//step 4 convert the Maven project to a Cucumber project, right click the bdd proj, configure, covert to cucumber proj ( a green ball will appear on the poj icon)
	//
}
